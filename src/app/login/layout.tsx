export const metadata = {
  title: "Login - Gastos Negocio",
  description: "Iniciar sesion",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
